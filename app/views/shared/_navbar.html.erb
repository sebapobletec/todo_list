<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="<%= root_path %> ">Todo 18!</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
    </ul>
    <ul class="navbar-nav mr-auto">
        <% if user_signed_in? %>
          Tasks completed: <%= @completed_todos %>/<%=@assignments.count %>
        <% end %>
    </ul>

    <ul class="navbar-nav">
      <% if user_signed_in? %>
        <li>Welcome, <%= current_user.name %>   <a class="btn btn-info" href="<%= destroy_user_session_path %>" data-method="DELETE">Log out</a></li>
      <% else %>
        <li><a class="btn btn-info" href="<%= new_user_registration_path %>">Sign Up</a></li>
      <% end %>
    </ul>

  </div>
</nav>
